# Types and definitions

```python
    DEFAULT_REMOTE_ADDRESS = 			'127.0.0.1'     # default remote server IP address
    DEFAULT_REMOTE_PORT = 				9002			# default remote receiver network port
```

Accessing defines:

```python
    from megamicros.MegamicrosWS import DEFAULT_REMOTE_ADDRESS, DEFAULT_REMOTE_PORT
```
